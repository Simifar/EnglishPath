import Link from 'next/link';
import { levels } from '@/data';
import { FavoritesButton } from '@/components/catalog/FavoritesButton';
import { CefrLevelIcon } from '@/components/content/CefrLevelIcon';
import { SiteFooter } from '@/components/layout/SiteFooter';
import { SiteHeader } from '@/components/layout/SiteHeader';
export const metadata = { title: 'Планы по уровням', description: 'Справочные планы CEFR A1–C2: темы, навыки и учебники для самостоятельного выбора материалов.', alternates: { canonical: '/plans' } };
export default function PlansPage() { return <><SiteHeader /><main className="mx-auto max-w-6xl px-4 py-12 sm:px-6"><p className="text-sm font-semibold text-primary">CEFR A1–C2</p><h1 className="mt-2 text-4xl font-bold tracking-tight">Планы по уровням</h1><p className="mt-4 max-w-2xl text-muted-foreground">Справочные карты тем, навыков и учебников. Это ориентир для самостоятельного выбора материалов, а не интерактивный курс.</p><div className="mt-10 grid gap-5 md:grid-cols-2">{levels.map((level) => <article key={level.id} className="rounded-2xl border border-border/80 bg-card p-6 shadow-sm"><div className="flex items-center gap-3"><span className="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-secondary text-primary"><CefrLevelIcon level={level.title} /></span><div><h2 className="text-xl font-semibold"><Link href={`/plans/${level.slug}`} className="hover:text-primary hover:underline">{level.title}: {level.fullName}</Link></h2><p className="mt-1 text-sm text-muted-foreground">Ориентир: {level.duration}</p></div></div><p className="mt-5 text-sm leading-6 text-muted-foreground">{level.description}</p><div className="mt-5 flex flex-wrap gap-3"><Link href={`/plans/${level.slug}`} className="text-sm font-semibold text-primary hover:underline">Открыть карту уровня →</Link><FavoritesButton contentId={level.id} /></div></article>)}</div></main><SiteFooter /></>; }
