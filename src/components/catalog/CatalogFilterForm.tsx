import Link from 'next/link';
import { cefrLevels, resourceCategories, type ResourceCategory } from '@/data';
import type { ResourceFilters, TextbookFilters } from '@/lib/catalog-filters';

const categoryLabels: Record<ResourceCategory, string> = { grammar: 'Грамматика', reading: 'Чтение', listening: 'Аудирование', speaking: 'Общение', dictionary: 'Словари', practice: 'Практика', pronunciation: 'Произношение', exams: 'Экзамены' };
const fieldClass = 'mt-1 min-h-11 w-full rounded-md border bg-background px-3 text-sm focus-visible:ring-2 focus-visible:ring-ring';
export function TextbookFilterForm({ filters, publishers, formats, purposes }: { filters: TextbookFilters; publishers: string[]; formats: string[]; purposes: string[] }) { return <form className="mt-8 grid gap-3 rounded-xl border bg-muted/30 p-4 sm:grid-cols-2 lg:grid-cols-4"><label className="text-sm font-medium">Уровень<select name="level" defaultValue={filters.level ?? ''} className={fieldClass}><option value="">Все уровни</option>{cefrLevels.map((level) => <option key={level}>{level}</option>)}</select></label><label className="text-sm font-medium">Издатель<select name="publisher" defaultValue={filters.publisher ?? ''} className={fieldClass}><option value="">Все издатели</option>{publishers.map((item) => <option key={item}>{item}</option>)}</select></label><label className="text-sm font-medium">Формат<select name="format" defaultValue={filters.format ?? ''} className={fieldClass}><option value="">Все форматы</option>{formats.map((item) => <option key={item}>{item}</option>)}</select></label><label className="text-sm font-medium">Назначение<select name="purpose" defaultValue={filters.purpose ?? ''} className={fieldClass}><option value="">Любое назначение</option>{purposes.map((item) => <option key={item}>{item}</option>)}</select></label><div className="flex flex-col gap-3 sm:col-span-2 sm:flex-row sm:items-end lg:col-span-4"><button className="min-h-11 w-full rounded-md bg-primary px-4 text-sm font-semibold text-primary-foreground hover:bg-primary/90 focus-visible:ring-2 sm:w-auto">Применить</button><Link href="/textbooks" className="py-2 text-center text-sm font-semibold text-primary hover:underline sm:text-left">Сбросить</Link></div></form>; }
export function ResourceFilterForm({ filters }: { filters: ResourceFilters }) { return <form className="mt-8 grid gap-3 rounded-xl border bg-muted/30 p-4 sm:grid-cols-[minmax(0,1fr)_auto_auto]"><label className="text-sm font-medium">Категория<select name="category" defaultValue={filters.category ?? ''} className={fieldClass}><option value="">Все категории</option>{resourceCategories.map((item) => <option value={item} key={item}>{categoryLabels[item]}</option>)}</select></label><label className="flex min-h-11 items-end gap-2 text-sm font-medium"><input type="checkbox" name="free" value="true" defaultChecked={filters.free} className="h-4 w-4" />Только бесплатные</label><div className="flex flex-col gap-3 sm:flex-row sm:items-end"><button className="min-h-11 w-full rounded-md bg-primary px-4 text-sm font-semibold text-primary-foreground hover:bg-primary/90 focus-visible:ring-2 sm:w-auto">Применить</button><Link href="/resources" className="py-2 text-center text-sm font-semibold text-primary hover:underline sm:text-left">Сбросить</Link></div></form>; }
