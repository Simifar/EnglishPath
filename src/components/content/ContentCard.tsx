import Link from 'next/link';
import type { ReactNode } from 'react';
import { TagList } from './TagList';
import { FavoritesButton } from '@/components/catalog/FavoritesButton';
export function ContentCard({ href, eyebrow, title, description, tags = [], favoriteId, children }: { href: string; eyebrow?: string; title: string; description: string; tags?: string[]; favoriteId?: string; children?: ReactNode }) { return <article className="group flex h-full flex-col rounded-xl border border-border bg-card p-5 transition-colors duration-150 hover:border-primary/40 hover:bg-accent/20"><>{eyebrow && <p className="text-xs font-bold tracking-wide text-primary">{eyebrow}</p>}<h2 className="mt-2 text-lg font-semibold tracking-tight"><Link href={href} className="rounded-sm hover:text-primary hover:underline focus-visible:outline-2">{title}</Link></h2><p className="mt-3 flex-1 text-sm leading-6 text-muted-foreground">{description}</p>{tags.length > 0 && <div className="mt-4"><TagList tags={tags} /></div>}{children}<div className="mt-5 flex flex-wrap items-center gap-3 border-t border-border pt-4"><Link href={href} className="inline-flex rounded-sm text-sm font-semibold text-primary hover:underline focus-visible:outline-2">Подробнее →</Link>{favoriteId && <FavoritesButton contentId={favoriteId} className="ml-auto px-2.5 py-1.5 text-xs" />}</div></></article>; }
