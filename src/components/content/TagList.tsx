export function TagList({ tags }: { tags: string[] }) { const visible = tags.slice(0, 3); const hidden = tags.length - visible.length; return <ul aria-label="Теги" className="flex flex-wrap gap-2">{visible.map((tag) => <li key={tag} className="rounded-full border border-border/70 bg-muted px-2.5 py-1 text-xs font-medium text-muted-foreground">{tag}</li>)}{hidden > 0 && <li className="rounded-full border border-border/70 bg-muted px-2.5 py-1 text-xs font-medium text-muted-foreground">+{hidden}</li>}</ul>; }
